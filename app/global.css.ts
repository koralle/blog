import { style } from '@vanilla-extract/css'

export const htmlRootStyles = style({
  height: '100%',
})

export const bodyRootStyles = style({
  height: '100%',
})

export const themeRootStyles = style({
  height: '100%',
})

export const gridRootStyles = style({})

export const headerRootStyles = style({})
